import axios from "axios";
import React, { useEffect, useRef } from "react";
import { toast } from "react-toastify";

const Clientes = () =>{

  return(
    <>
	  <h2 className="text-center mt-4">Cadastro de Clientes</h2>
	</>
  )
}

export default Clientes;